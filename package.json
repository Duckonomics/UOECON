{
  "name": "uo-econ-interactive-paper-template",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/paper-config.js && node --check public/app.js && node --check public/site-config.js"
  },
  "devDependencies": {
    "wrangler": "^4.133.0"
  }
}

